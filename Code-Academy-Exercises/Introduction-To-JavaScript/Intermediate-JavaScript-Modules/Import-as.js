import {availableAirplanes, flightRequirements, meetsStaffRequirements, meetsSpeedRangeRequirements} from './airplane';

function displayFuelCapacity() {
  availableAirplanes.forEach(function(element)
{

function displayStaffStatus() {
availableAirplanes.forEach(function(element)
{

function displaySpeedRangeStatus() {
  availableAirplanes.forEach(function(element)
}

flightRequirements['requiredStaff']) );

flightRequirements['requiredSpeedRange']));

meetsStaffRequirements(element['availableStaff']

meetsSpeedRangeRequirements(element['maxSpeed'],



import{aircrafts,flightReqs,meetsStaffReqs,meetsSpeedRangeReqs} from './airplane'

function displayFuelCapacity() {
  aircrafts.forEach(function(element)
}

function displayStaffStatus() {
  aircrafts.forEach(function(element)
}

function displaySpeedRangeStatus() {
  aircrafts.forEach(function(element)
}

flightReqs['requiredStaff']) );

flightReqs['requiredSpeedRange']));

meetsStaffReqs(element['availableStaff']

meetsSpeedRangeReqs(element['maxSpeed'],
